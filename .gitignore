# Your real config, profile and session are personal — never commit them.
config.yaml
profile.yaml
# ...but the test fixture's config is part of the repo.
!tests/fixtures/site/config.yaml
state/
*.db
.env

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/
